function FilterSectionLabel({ children }) {
  return (
    <div className="mb-[7px] text-[9px] font-medium uppercase tracking-[0.1em] text-[var(--muted)]">
      {children}
    </div>
  )
}

export default FilterSectionLabel
