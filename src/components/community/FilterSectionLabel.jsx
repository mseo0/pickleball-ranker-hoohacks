function FilterSectionLabel({ children }) {
  return (
    <div className="mb-[7px] text-[9px] uppercase tracking-[0.08em] text-[var(--muted)]">
      {children}
    </div>
  )
}

export default FilterSectionLabel
